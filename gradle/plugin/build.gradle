plugins {
    id 'java-gradle-plugin'
}

group = maven_group
version = project_version

repositories {
    mavenLocal()
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = "8.1.1"
}

